{
  "version": 2,
  "buildCommand": "bash build.sh",
  "outputDirectory": "dist",
  "framework": "vite",
  "builds": [
    {
      "src": "build.sh",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    }
  ]
}
